import styled from "styled-components";

const Profile = () => {
  return (
    <Container>
      <h1>Profile</h1>
    </Container>
  );
};

const Container = styled.div`
  padding: 2rem;
`;

export default Profile;