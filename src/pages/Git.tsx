import styled from "styled-components";

const Git = () => {
  return (
    <Container>
      <h1>Git</h1>
    </Container>
  );
};

const Container = styled.div`
  padding: 2rem;
`;

export default Git;